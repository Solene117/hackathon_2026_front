# URL du backend (ex. http://localhost:3000 en local)
# En prod : URL publique du back (ex. https://api.example.com), injectée via secret GitHub VITE_API_BASE_URL
VITE_API_BASE_URL=http://localhost:3000
